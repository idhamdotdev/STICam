<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera access for the encoding pipeline -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Network server (streams to Windows host) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Keep CPU/screen alive during streaming -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- MP4 recording to Movies/STICam/ -->
    <!-- API ≤28: needs WRITE_EXTERNAL_STORAGE for direct file writes       -->
    <!-- API 29+: no permission needed for Environment.DIRECTORY_MOVIES     -->
    <!-- API 33+: READ_MEDIA_VIDEO to read back saved clips                 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"
        tools:targetApi="33" />

    <!-- Foreground service (type camera) keeps streaming alive when the app
         is backgrounded — see StreamingService -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <!-- API 33+: show the "streaming" notification (optional — stream works without it) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Declare camera hardware requirement -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sticam"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustNothing">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Holds camera access while streaming in the background -->
        <service
            android:name=".StreamingService"
            android:exported="false"
            android:foregroundServiceType="camera" />
    </application>

</manifest>
